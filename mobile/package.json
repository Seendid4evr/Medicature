{
  "name": "medicature-app",
  "version": "1.0.0",
  "description": "Medicature Mobile App - Capacitor wrapper for the Medicature web platform",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed - uses existing web app'",
    "android": "npx cap run android",
    "ios": "npx cap run ios",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "@capacitor/push-notifications": "^5.0.0",
    "@capacitor/local-notifications": "^5.0.0",
    "@capacitor/network": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
